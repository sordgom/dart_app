name: project
description: A new Flutter application.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg : ^0.22.0
  flutter_awesome_buttons: ^0.1.0
  wifi_iot: ^0.3.3
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/Ellipse1.png
    - assets/vector.svg

fonts:
  - family: Quando
    fonts:
      - asset: assets/Quando-Regular.ttf
      - weight: 400

